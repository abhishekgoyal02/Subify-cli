# Website Development

Install frontend dependencies:

```sh
npm install
```

Start the development server:

```sh
npm run dev
```

Build the frontend:

```sh
npm run build
```

Preview the production build:

```sh
npm run preview
```
